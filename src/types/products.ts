export type ProductType = {
  label: string;
  desc?: string;
  price?: string;
  img: string;
  btnText?: string;
  className?: string;
};
export type TitleType = {
  subtitle: string;
  title: string;
};
